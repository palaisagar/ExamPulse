<nav class="navbar navbar-expand-lg premium-navbar">
  <div class="container">

    <svg class="theme-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 -960 960 960" fill="currentColor" width="50" height="50">
      <path d="M479-120 189-279v-240L40-600l439-240 441 240v317h-60v-282l-91 46v240L479-120Zm0-308 315-172-315-169-313 169 313 172Zm0 240 230-127v-168L479-360 249-485v170l230 127Z" />
    </svg>

    &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;

    <%
      let homeLink = "/";
      if (currentUser) {
        if (currentUser.role === "admin") {
          homeLink = "/admin/dashboard";
        } else if (currentUser.role === "teacher") {
          homeLink = "/teacher/dashboard";
        } else {
          homeLink = "/student/dashboard";
        }
      }
    %>

    <a class="navbar-brand fw-bold text-white" href="<%= homeLink %>">
      Examᴘᴜʟꜱᴇ
    </a>

    <button class="navbar-toggler bg-light" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navMenu">
      <ul class="navbar-nav ms-auto align-items-lg-center gap-2">

        <li class="nav-item">
          <a class="nav-link text-white" href="/">Home</a>
        </li>

        <li class="nav-item">
          <button type="button" class="btn btn-outline-light btn-sm" id="themeToggle">
            🌙 ☀️
          </button>
        </li>

        <% if (!currentUser) { %>

          <li class="nav-item">
            <a class="nav-link text-white" href="/login">Login</a>
          </li>

          <li class="nav-item">
            <a class="btn btn-light rounded-pill px-4" href="/register">
              Register
            </a>
          </li>

          <li class="nav-item">
            <a class="btn btn-danger rounded-pill px-3" href="/admin/login">Admin</a>
          </li>

        <% } else { %>

          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-white d-flex align-items-center gap-2" href="#" role="button" data-bs-toggle="dropdown">

              <% if (currentUser.profilePhoto) { %>
                <img src="<%= currentUser.profilePhoto %>" alt="Profile" class="profile-avatar-img">
              <% } else { %>
                <span class="user-avatar">
                  <%= currentUser.name ? currentUser.name.charAt(0).toUpperCase() : "U" %>
                </span>
              <% } %>

              <div class="d-flex flex-column lh-sm">
                <span><%= currentUser.name %></span>
                <small class="role-badge-text"><%= currentUser.role %></small>
              </div>
            </a>

            <ul class="dropdown-menu dropdown-menu-end premium-dropdown">

              <% if (currentUser.role === "admin") { %>
                <li><a class="dropdown-item" href="/admin/dashboard">Admin Dashboard</a></li>
                <li><a class="dropdown-item" href="/admin/profile">My Profile</a></li>
                <li><a class="dropdown-item text-warning" href="#">System Control</a></li>
              <% } %>

              <% if (currentUser.role === "teacher") { %>
                <li><a class="dropdown-item" href="/teacher/dashboard">Teacher Dashboard</a></li>
                <li><a class="dropdown-item" href="/teacher/profile">My Profile</a></li>
              <% } %>

              <% if (currentUser.role === "student") { %>
                <li><a class="dropdown-item" href="/student/dashboard">Student Dashboard</a></li>
                <li><a class="dropdown-item" href="/student/profile">My Profile</a></li>
              <% } %>

              <li><hr class="dropdown-divider"></li>

              <li>
                <a class="dropdown-item text-danger" href="/auth/logout">
                  Logout
                </a>
              </li>

            </ul>
          </li>

        <% } %>

      </ul>
    </div>
  </div>
</nav>